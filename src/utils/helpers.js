export function formatTime(d){if(!d)return"";return new Date(d).toLocaleTimeString("tr-TR",{hour:"2-digit",minute:"2-digit"})}
export function formatDate(d){if(!d)return"";const date=new Date(d);const now=new Date();const m=Math.floor((now-date)/60000);if(m<1)return"simdi";if(m<60)return m+" dk";const h=Math.floor(m/60);if(h<24)return h+" sa";return date.toLocaleDateString("tr-TR",{day:"numeric",month:"short"})}
export function getInitials(n){if(!n)return"?";return n.split(" ").map(x=>x[0]).join("").toUpperCase().slice(0,2)}
export function truncate(s,l=40){if(!s)return"";return s.length>l?s.slice(0,l)+"...":s}
export function fileToBase64(f){return new Promise((res,rej)=>{const r=new FileReader();r.readAsDataURL(f);r.onload=()=>res(r.result);r.onerror=e=>rej(e)})}
export function playSound(type){try{const ctx=new(window.AudioContext||window.webkitAudioContext)();const o=ctx.createOscillator();const g=ctx.createGain();o.connect(g);g.connect(ctx.destination);if(type==="msg"){o.frequency.setValueAtTime(800,ctx.currentTime);o.frequency.setValueAtTime(600,ctx.currentTime+0.1);g.gain.setValueAtTime(0.3,ctx.currentTime);g.gain.exponentialRampToValueAtTime(0.01,ctx.currentTime+0.2);o.start(ctx.currentTime);o.stop(ctx.currentTime+0.2);if(navigator.vibrate)navigator.vibrate(100)}else if(type==="call"){o.frequency.setValueAtTime(440,ctx.currentTime);g.gain.setValueAtTime(0.5,ctx.currentTime);o.start(ctx.currentTime);o.stop(ctx.currentTime+0.5);if(navigator.vibrate)navigator.vibrate([200,100,200,100,200])}}catch(e){}}
export const EMOJIS=["😀","😂","🤣","😊","😍","🥰","😘","😎","🤔","😮","😢","😡","👍","👎","❤️","🔥","🎉","💯","👏","🙏","💪","✨","🌟","💫","🎈","🎁","☕","🍕","⚽","🎮","🎵","📸","💬","✅","❌","⭐"]
